---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: public
spec:
  podSelector:
    matchLabels:
      network: public
  ingress:
    - from:
        - podSelector:
            matchLabels:
              network: public

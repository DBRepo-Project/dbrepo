---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: public
  namespace: {{ include "common.names.namespace" . | quote }}
spec:
  podSelector:
    matchLabels:
      network: public
  ingress:
    - from:
        - podSelector:
            matchLabels:
              network: public

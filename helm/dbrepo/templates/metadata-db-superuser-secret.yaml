{{- if .Values.metadatadb.enabled }}
---
apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: metadata-db-superuser-secret
  namespace: {{ include "common.names.namespace" . | quote }}
stringData:
  username: "{{ .Values.metadatadb.superUser.user }}"
  password: "{{ .Values.metadatadb.superUser.password }}"
{{- end }}

variables:
  BUILD_VERSION: ""
  HOSTALIASES: "./hosts"
  DOCKER_HOST: "unix:///var/run/dind/docker.sock"
  TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE: "/var/run/dind/docker.sock"
  DOCKER_VERSION: "27"
  ALPINE_VERSION: "3.21"
  PYTHON_VERSION: "3.11"
  JAVA_VERSION: "21"
  NODE_VERSION: "24"
  REGCLIENT_VERSION: "0.8.2"
  README_GENERATOR_VERSION: "2.8.1"
  BUN_VERSION: "1.2.21"
  DOC_VERSION: "1.13"
  APP_VERSION: "1.13.4"
  DUCKDB_VERSION: "1.2.2"
  CHART_VERSION: "1.13.3"
  SUPPORTED_VERSIONS: "1.13.0, 1.13.1, 1.13.2, 1.13.3, 1.13.4"
  SUPPORTED_ARCH: "linux/amd64"
  MAINTAINED_SERVICES: "dashboard-service, dashboard-service-init, data-service, consumer-service, metadata-service, search-service, search-service-init, storage-service-init, ui"
  CACHE_FALLBACK_KEY: "${CI_DEFAULT_BRANCH}"
  # This will supress any download for dependencies and plugins or upload messages which would clutter the console log.
  # `showDateTime` will show the passed time in milliseconds. You need to specify `--batch-mode` to make this work.
  MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true -Dstyle.color=always"
  # As of Maven 3.3.0 instead of this you may define these options in `.mvn/maven.config` so the same config is used
  # when running from the command line.
  # `installAtEnd` and `deployAtEnd` are only effective with recent version of the corresponding plugins.
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version -DinstallAtEnd=true -DdeployAtEnd=true"

image: alpine:${ALPINE_VERSION}

# Cache downloaded dependencies and plugins between builds.
# To keep cache across branches add 'key: "$CI_JOB_NAME"'
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .m2/repository

workflow:
  rules:
    - if: $CI_COMMIT_REF_NAME == "dev"
      variables:
        BUILD_VERSION: "-dev.git.${CI_COMMIT_SHORT_SHA}"
    - when: always

stages:
  - lint
  - build
  - test
  - docs
  - clean
  - release
  - verify
  - scan

lint-docker-compose:
  image: docker.io/alpine:${ALPINE_VERSION}
  stage: lint
  variables:
    VERSION: 4.45.1
    BINARY: yq_linux_amd64
  before_script:
    - 'apk add --no-cache bash wget'
    - 'wget https://github.com/mikefarah/yq/releases/download/v${VERSION}/${BINARY} -O /usr/bin/yq && chmod +x /usr/bin/yq'
  script:
    - "diff <(yq '.volumes' docker-compose.yml) <(yq '.volumes' .docker/docker-compose.yml)"
    - "IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-auth-db'"
    - "IGNORE_IMAGE=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-auth-service'"
    - "IGNORE_IMAGE=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-broker-service'"
    - "IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-dashboard-service'"
    - "bash .scripts/check-service.sh 'dbrepo-data-db'"
    - "IGNORE_VOLUMES=1 bash .scripts/check-service.sh 'dbrepo-data-db-metrics'"
    - "IGNORE_IMAGE=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-data-service'"
    - "IGNORE_VOLUMES=1 bash .scripts/check-service.sh 'dbrepo-gateway-service'"
    - "IGNORE_VOLUMES=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-identity-service'"
    - "IGNORE_VOLUMES=1 bash .scripts/check-service.sh 'dbrepo-metadata-db'"
    - "IGNORE_VOLUMES=1 bash .scripts/check-service.sh 'dbrepo-metadata-db-metrics'"
    - "IGNORE_IMAGE=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-metadata-service'"
    - "IGNORE_VOLUMES=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-metric-db'"
    - "bash .scripts/check-service.sh 'dbrepo-search-db'"
    - "IGNORE_IMAGE=1 IGNORE_PORTS=1 bash .scripts/check-service.sh 'dbrepo-search-service'"
    - "IGNORE_IMAGE=1 bash .scripts/check-service.sh 'dbrepo-search-service-init'"
    - "IGNORE_VOLUMES=1 bash .scripts/check-service.sh 'dbrepo-storage-service'"
    - "IGNORE_VOLUMES=1 IGNORE_IMAGE=1 bash .scripts/check-service.sh 'dbrepo-storage-service-init'"
    - "IGNORE_IMAGE=1 bash .scripts/check-service.sh 'dbrepo-dashboard-service-init'"
    - "IGNORE_IMAGE=1 bash .scripts/check-service.sh 'dbrepo-ui'"

lint-helm-schema:
  image: docker.io/alpine:${ALPINE_VERSION}
  stage: lint
  variables:
    VERSION: 4.45.1
    BINARY: yq_linux_amd64
  before_script:
    - apk add --no-cache helm git bash wget
    - 'wget https://github.com/mikefarah/yq/releases/download/v${VERSION}/${BINARY} -O /usr/bin/yq && chmod +x /usr/bin/yq'
    - diff <(yq '.appVersion' ./helm/dbrepo/Chart.yaml) <(echo "${APP_VERSION}")
    - diff <(yq '.version' ./helm/dbrepo/Chart.yaml) <(echo "${APP_VERSION}")
    - helm plugin install https://github.com/losisin/helm-values-schema-json.git
    - helm package ./helm/seaweedfs --destination ./build
    - helm registry login --username "${CI_REGISTRY_USER}" --password "${CI_REGISTRY_PASSWORD}" "${CI_REGISTRY_URL}"
    - helm registry login --username "${CI_REGISTRY2_USER}" --password "${CI_REGISTRY2_PASSWORD}" "${CI_REGISTRY2_URL}"
  script:
    - helm lint ./helm/dbrepo ./helm/seaweedfs
    - helm schema --values ./helm/dbrepo/values.yaml --output ./helm/dbrepo/control-values.schema.json
    - helm schema --values ./helm/seaweedfs/values.yaml --output ./helm/seaweedfs/control-values.schema.json
    - diff ./helm/dbrepo/values.schema.json ./helm/dbrepo/control-values.schema.json
    - diff ./helm/seaweedfs/values.schema.json ./helm/seaweedfs/control-values.schema.json
  artifacts:
    when: always
    paths:
      - ./helm/dbrepo/values.schema.json
    expire_in: 1 days

lint-helm-readme:
  image: docker.io/node:${NODE_VERSION}-alpine${ALPINE_VERSION}
  stage: lint
  before_script:
    - apk add --no-cache alpine-sdk bash git
    - cp ./helm/dbrepo/README.md ./CI_README.md
    - git clone https://github.com/bitnami/readme-generator-for-helm
    - (cd ./readme-generator-for-helm && git checkout $README_GENERATOR_VERSION && npm install && npm install -g pkg && pkg . -t node*-alpine-x64 -o /usr/local/sbin/readme-generator)
  script:
    - readme-generator --readme ./helm/dbrepo/README.md --values ./helm/dbrepo/values.yaml
    - diff ./CI_README.md ./helm/dbrepo/README.md
  artifacts:
    when: always
    paths:
      - ./helm/dbrepo/README.md
    expire_in: 1 days

lint-java-lib:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: lint
  script:
    - mvn -f ./lib/java/dbrepo-core/pom.xml dependency:analyze

lint-data-service:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: lint
  only:
    - merge_requests
    - master
  before_script:
    - "mvn -f ./lib/java/dbrepo-core/pom.xml clean package install $MAVEN_OPTS -DskipTests"
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-data-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - mvn -f ./dbrepo-data-service/pom.xml dependency:analyze

lint-metadata-service:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: lint
  only:
    - merge_requests
    - master
  before_script:
    - "mvn -f ./lib/java/dbrepo-core/pom.xml clean package install $MAVEN_OPTS -DskipTests"
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-metadata-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - mvn -f ./dbrepo-metadata-service/pom.xml dependency:analyze

lint-consumer-service:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: lint
  only:
    - merge_requests
    - master
  before_script:
    - "mvn -f ./lib/java/dbrepo-core/pom.xml clean package install $MAVEN_OPTS -DskipTests"
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-consumer-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - mvn -f ./dbrepo-consumer-service/pom.xml dependency:analyze

lint-helm-files:
  image: docker.io/alpine:${ALPINE_VERSION}
  stage: lint
  script:
    - diff ./dbrepo-metadata-db/1_setup-schema.sql ./helm/dbrepo/files/01-setup-schema.sql

build-metadata-service:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - lint-metadata-service
  dependencies:
    - build-java-lib
    - lint-metadata-service
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-metadata-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - "mvn -f ./dbrepo-metadata-service/pom.xml -q clean package $MAVEN_OPTS -DskipTests"
  # Compiled classes are needed for SonarQube in later stages
  artifacts:
    when: always
    paths:
      - ./dbrepo-metadata-service/oai/target/classes
      - ./dbrepo-metadata-service/repositories/target/classes
      - ./dbrepo-metadata-service/repositories/target/generated-sources
      - ./dbrepo-metadata-service/rest-service/target/classes
      - ./dbrepo-metadata-service/services/target/classes
    expire_in: 1 days

build-consumer-service:
  image: docker.io/maven:3-amazoncorretto-${JAVA_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - lint-consumer-service
  dependencies:
    - build-java-lib
    - lint-consumer-service
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-consumer-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - "mvn -f ./dbrepo-consumer-service/pom.xml -q clean package $MAVEN_OPTS -DskipTests"
  # Compiled classes are needed for SonarQube in later stages
  artifacts:
    when: always
    paths:
      - ./dbrepo-consumer-service/amqp-service/target/classes
      - ./dbrepo-consumer-service/services/target/classes
    expire_in: 1 days

build-python-lib:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: build
  only:
    - merge_requests
    - master
    - /^release-.*/
    - tags
  before_script:
    - "apk --no-cache add python3-dev"
    - "python -m pip install build"
  script:
    - "python3 -m build --sdist ./lib/python"
    - "python3 -m build --wheel ./lib/python"
  artifacts:
    when: always
    paths:
      - ./lib/python/dist

build-java-lib:
  image: maven:3-amazoncorretto-${JAVA_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  needs:
    - lint-java-lib
  dependencies:
    - lint-java-lib
  script:
    - "mvn -f ./lib/java/dbrepo-core/pom.xml clean package install $MAVEN_OPTS -DskipTests"
  artifacts:
    when: always
    paths:
      - ./lib/java/dbrepo-core/target/classes
      - ./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar

build-data-service:
  image: maven:3-amazoncorretto-${JAVA_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - lint-data-service
  dependencies:
    - build-java-lib
    - lint-data-service
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-data-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - "mvn -f ./dbrepo-data-service/pom.xml -q clean package $MAVEN_OPTS -DskipTests"
  # Compiled classes are needed for SonarQube in later stages
  artifacts:
    when: always
    paths:
      - ./dbrepo-data-service/querystore/target/classes
      - ./dbrepo-data-service/rest-service/target/classes
      - ./dbrepo-data-service/rest-service/target/test-classes
      - ./dbrepo-data-service/services/target/classes
      - ./dbrepo-data-service/services/target/generated-sources
    expire_in: 1 days

build-ui:
  image: oven/bun:${BUN_VERSION}-alpine
  stage: build
  only:
    - merge_requests
    - master
  script:
    - "cd ./dbrepo-ui && bun install && bun run build"

build-search-service:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  variables:
    PIPENV_PIPFILE: "./dbrepo-search-service/Pipfile"
  needs:
    - build-python-lib
  dependencies:
    - build-python-lib
  before_script:
    - "cp ./lib/python/dist/dbrepo-$APP_VERSION.tar.gz ./dbrepo-search-service/lib/dbrepo-$APP_VERSION.tar.gz"
    - "pip install pipenv"
  script:
    - "pipenv install --system --deploy"

build-dashboard-service:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: build
  only:
    - merge_requests
    - master
  variables:
    PIPENV_PIPFILE: "./dbrepo-dashboard-service/Pipfile"
  needs:
    - build-python-lib
  dependencies:
    - build-python-lib
  before_script:
    - "cp ./lib/python/dist/dbrepo-$APP_VERSION.tar.gz ./dbrepo-dashboard-service/lib/dbrepo-$APP_VERSION.tar.gz"
    - "pip install pipenv"
  script:
    - "pipenv install --system --deploy"

build-images:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: build
  only:
    - merge_requests
    - master
  variables:
    DOCKER_BUILDKIT: 1
  needs:
    - build-java-lib
    - build-python-lib
  dependencies:
    - build-java-lib
    - build-python-lib
  before_script:
    - "apk add --no-cache make maven openjdk21-jdk"
    - "cp -r ./lib/python/dist/dbrepo-$APP_VERSION* ./dbrepo-search-service/lib"
    - "cp -r ./lib/python/dist/dbrepo-$APP_VERSION* ./dbrepo-dashboard-service/lib"
    - "mvn deploy:deploy-file -q -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-consumer-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
    - "mvn deploy:deploy-file -q -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-data-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
    - "mvn deploy:deploy-file -q -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-metadata-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
    - echo "$CI_REGISTRY_PASSWORD" | docker login --username "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY_URL
  script:
    - "docker compose build"

build-helm:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: build
  only:
    - merge_requests
    - tags
  before_script:
    - "apk add --no-cache helm make"
    - echo "$CI_GPG_KEYRING" | base64 -d > ./secring.gpg
    - echo "$CI_REGISTRY_PASSWORD" | docker login --username "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY_URL
  script:
    - make build-helm

test-consumer-service:
  image: maven:3-amazoncorretto-${JAVA_VERSION}
  stage: test
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - build-consumer-service
  dependencies:
    - build-java-lib
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-consumer-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - "mvn -f ./dbrepo-consumer-service/pom.xml clean test verify $MAVEN_OPTS"
    - "cat ./dbrepo-consumer-service/report/target/site/jacoco-aggregate/index.html | grep -o 'Total[^%]*%' | sed 's/<.*>/ /; s/Total/Jacoco Coverage Total:/'"
  artifacts:
    when: always
    paths:
      - ./dbrepo-consumer-service/report/target/site/jacoco-aggregate/
      - ./dbrepo-consumer-service/amqp-service/target/surefire-reports/
    expire_in: 1 days
    reports:
      junit: ./dbrepo-consumer-service/amqp-service/target/surefire-reports/TEST-*.xml
  coverage: '/Total.*?([0-9]{1,3})%/'

test-metadata-service:
  image: maven:3-amazoncorretto-${JAVA_VERSION}
  stage: test
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - build-metadata-service
  dependencies:
    - build-java-lib
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-metadata-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
  script:
    - "mvn -f ./dbrepo-metadata-service/pom.xml clean test verify $MAVEN_OPTS"
    - "cat ./dbrepo-metadata-service/report/target/site/jacoco-aggregate/index.html | grep -o 'Total[^%]*%' | sed 's/<.*>/ /; s/Total/Jacoco Coverage Total:/'"
  artifacts:
    when: always
    paths:
      - ./dbrepo-metadata-service/report/target/site/jacoco-aggregate/
      - ./dbrepo-metadata-service/rest-service/target/surefire-reports/
    expire_in: 1 days
    reports:
      junit: ./dbrepo-metadata-service/rest-service/target/surefire-reports/TEST-*.xml
  coverage: '/Total.*?([0-9]{1,3})%/'

test-data-service:
  image: maven:3-amazoncorretto-${JAVA_VERSION}
  stage: test
  services:
    - minio/minio:latest
  variables:
    SPRING_PROFILES_ACTIVE: ci
  only:
    - merge_requests
    - master
  needs:
    - build-java-lib
    - build-data-service
  dependencies:
    - build-java-lib
  before_script:
    - "mvn deploy:deploy-file $MAVEN_OPTS -Dfile=./lib/java/dbrepo-core/target/dbrepo-core-$APP_VERSION.jar -DgroupId=at.ac.tuwien.ifs.dbrepo -DartifactId=dbrepo-core -Dversion=$APP_VERSION -Dpackaging=jar -Durl=file:./dbrepo-data-service/lib/ -DrepositoryId=maven-repository -DupdateReleaseInfo=true"
    - "mkdir -p ~/.duckdb/extensions/$DUCKDB_VERSION/linux_amd64"
    - "gunzip -r ./dbrepo-data-service/lib"
    - "mv ./dbrepo-data-service/lib/* ~/.duckdb/extensions/$DUCKDB_VERSION/linux_amd64"
  script:
    - "mvn -f ./dbrepo-data-service/pom.xml clean test verify $MAVEN_OPTS"
    - "cat ./dbrepo-data-service/report/target/site/jacoco-aggregate/index.html | grep -o 'Total[^%]*%' | sed 's/<.*>/ /; s/Total/Jacoco Coverage Total:/'"
  artifacts:
    when: always
    paths:
      - ./dbrepo-data-service/report/target/site/jacoco-aggregate/
      - ./dbrepo-data-service/rest-service/target/surefire-reports/
    expire_in: 1 days
    reports:
      junit: ./dbrepo-data-service/rest-service/target/surefire-reports/TEST-*.xml
  coverage: '/Total.*?([0-9]{1,3})%/'

test-search-service:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: test
  only:
    - merge_requests
    - master
  variables:
    PIPENV_PIPFILE: "./dbrepo-search-service/Pipfile"
  needs:
    - build-python-lib
    - build-search-service
  dependencies:
    - build-python-lib
    - build-search-service
  before_script:
    - "cp ./lib/python/dist/dbrepo-$APP_VERSION.tar.gz ./dbrepo-search-service/lib/dbrepo-$APP_VERSION.tar.gz"
    - "pip install pipenv"
    - "pipenv install --dev --system --deploy"
    - "mkdir -p /var/log/app/service/search"
  script:
    - cd ./dbrepo-search-service/ && coverage run --rcfile=.coveragerc -m pytest tests/test_app.py && coverage html && coverage xml && coverage report > ./coverage.txt
    - "cat ./coverage.txt | grep -o 'TOTAL[^%]*%'"
  artifacts:
    when: always
    paths:
      - /var/log/app/service/search/app.log
      - ./dbrepo-search-service/coverage.xml
      - ./dbrepo-search-service/coverage.txt
    expire_in: 1 days
    reports:
      junit: ./dbrepo-search-service/coverage.xml
  coverage: '/TOTAL.*?([0-9]{1,3})%/'

test-dashboard-service:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: test
  only:
    - merge_requests
    - master
  variables:
    PIPENV_PIPFILE: "./dbrepo-dashboard-service/Pipfile"
  needs:
    - build-python-lib
    - build-dashboard-service
  dependencies:
    - build-python-lib
    - build-dashboard-service
  before_script:
    - "cp ./lib/python/dist/dbrepo-$APP_VERSION.tar.gz ./dbrepo-dashboard-service/lib/dbrepo-$APP_VERSION.tar.gz"
    - "pip install pipenv"
    - "pipenv install --dev --system --deploy"
    - "mkdir -p /var/log/app/service/dashboard"
  script:
    - cd ./dbrepo-dashboard-service/ && coverage run --rcfile=.coveragerc -m pytest tests/test_integration_app.py && coverage html && coverage xml && coverage report > ./coverage.txt
    - "cat ./coverage.txt | grep -o 'TOTAL[^%]*%'"
  artifacts:
    when: always
    paths:
      - /var/log/app/service/dashboard/app.log
      - ./dbrepo-dashboard-service/coverage.xml
      - ./dbrepo-dashboard-service/coverage.txt
    expire_in: 1 days
    reports:
      junit: ./dbrepo-dashboard-service/coverage.xml
  coverage: '/TOTAL.*?([0-9]{1,3})%/'

test-python-lib:
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  stage: test
  only:
    - merge_requests
    - master
  variables:
    PIPENV_PIPFILE: "./lib/python/Pipfile"
  needs:
    - build-python-lib
  dependencies:
    - build-python-lib
  script:
    - "pip install pipenv"
    - "pipenv install --dev --system --deploy"
    - cd ./lib/python/ && coverage run --rcfile=.coveragerc -m pytest tests/test_unit_container.py tests/test_unit_messages.py tests/test_unit_image.py tests/test_unit_concept.py tests/test_unit_database.py tests/test_unit_identifier.py tests/test_unit_license.py tests/test_unit_query.py tests/test_unit_rest_client.py tests/test_unit_table.py tests/test_unit_user.py tests/test_unit_view.py tests/test_unit_unit.py && coverage html && coverage xml && coverage report > ./coverage.txt
    - "cat ./coverage.txt | grep -o 'TOTAL[^%]*%'"
  artifacts:
    when: always
    paths:
      - ./lib/python/coverage.xml
      - ./lib/python/coverage.txt
    expire_in: 1 days
    reports:
      junit: ./lib/python/coverage.xml
  coverage: '/TOTAL.*?([0-9]{1,3})%/'

test-storage-service-init:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: test
  only:
    - merge_requests
    - master
  before_script:
    - "apk --no-cache add s3cmd bash libxml2-utils"
    - "docker compose build dbrepo-storage-service-init"
    - "mkdir -p /app/config /var/log/app/service/storage"
    - "echo ${CI_S3_CONFIG} | base64 -d > /app/.s3cfg"
  script:
    - "bash ./dbrepo-storage-service/init/tests/test_integration_expire.sh"

test-broker-service:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: test
  only:
    - merge_requests
    - master
  before_script:
    - "apk --no-cache add bash"
  script:
    - "bash ./dbrepo-broker-service/tests/test_integration_feature_flags.sh"

test-ui:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: test
  only:
    - merge_requests
    - master
  needs:
    - build-ui
  dependencies:
    - build-ui
  before_script:
    - "apk add --no-cache bash apache2-utils"
    - "docker compose build dbrepo-ui"
    - "docker run --name dbrepo-ui -e NODE_OPTIONS='--max_old_space_size=256' -p 3000:3000 -d dbrepo-ui:latest"
  script:
    - "sleep 30"
    - "ENDPOINT=http://localhost:3000 bash ./dbrepo-ui/tests/test_heap.sh"

release-images:
  stage: release
  image: docker:${DOCKER_VERSION}-dind
  only:
    - tags
  variables:
    REPOSITORY_URL: "$CI_REGISTRY2_URL"
  dependencies:
    - build-python-lib
  needs:
    - build-python-lib
  before_script:
    - "apk add --no-cache make maven openjdk21-jdk bash"
    - "docker logout ${CI_REGISTRY_URL}"
    - "echo ${CI_REGISTRY_PASSWORD} | docker login --username ${CI_REGISTRY_USER} --password-stdin ${CI_REGISTRY_URL}"
    - "docker logout ${CI_REGISTRY2_URL}"
    - "echo ${CI_REGISTRY2_PASSWORD} | docker login --username ${CI_REGISTRY2_USER} --password-stdin ${CI_REGISTRY2_URL}"
  script:
    - "make release-images"
    - "APP_VERSION=$DOC_VERSION make release-images"
    - "make release-jupyter-image"
    - "APP_VERSION=$DOC_VERSION make release-jupyter-image"

release-helm:
  stage: release
  image: docker:${DOCKER_VERSION}-dind
  only:
    - tags
  needs:
    - build-helm
  dependencies:
    - build-helm
  before_script:
    - "apk add --no-cache sed helm make"
    - "docker logout ${CI_REGISTRY_URL}"
    - "echo ${CI_REGISTRY_PASSWORD} | docker login --username ${CI_REGISTRY_USER} --password-stdin ${CI_REGISTRY_URL}"
    - "docker logout ${CI_REGISTRY2_URL}"
    - "echo ${CI_REGISTRY2_PASSWORD} | docker login --username ${CI_REGISTRY2_USER} --password-stdin ${CI_REGISTRY2_URL}"
    - echo "$CI_GPG_SECRING" | base64 -d > ~/secring.gpg
    - echo "$CI_GPG_PUBRING" | base64 -d > ~/pubring.gpg
    - helm registry login --username "${CI_REGISTRY_USER}" --password "${CI_REGISTRY_PASSWORD}" "${CI_REGISTRY_URL}"
    - helm registry login --username "${CI_REGISTRY2_USER}" --password "${CI_REGISTRY2_PASSWORD}" "${CI_REGISTRY2_URL}"
    - sed -r "s/^(version:\s*).*/\1\"${APP_VERSION}\"/" -i ./helm/dbrepo/Chart.yaml
    - sed -r "s/^(appVersion:\s*).*/\1\"${APP_VERSION}\"/" -i ./helm/dbrepo/Chart.yaml
    - make build-helm
    - "helm package ./helm/dbrepo --sign --key 'Martin Weise' --keyring ~/secring.gpg --destination ./build"
    - "helm plugin install https://github.com/sigstore/helm-sigstore"
  script:
    - "helm push ./build/dbrepo-${CHART_VERSION}.tgz oci://${CI_REGISTRY2_URL}/helm"
    - "helm sigstore upload --keyring ~/secring.gpg ./build/dbrepo-${CHART_VERSION}.tgz"

release-docs:
  stage: release
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  only:
    - /^release-.*/
  needs:
    - build-python-lib
  dependencies:
    - build-python-lib
  before_script:
    - "apk add --no-cache alpine-sdk bash sed wget openssh jq curl"
    - "pip install pipenv furo sphinx ./lib/python/dist/dbrepo-$APP_VERSION.tar.gz"
    - "pipenv install --dev --system --deploy"
    - "mkdir -p ./final/${DOC_VERSION}/rest"
    - "mkdir -p ./final/${APP_VERSION}"
    - git config --global user.name "$CI_TECH_LEAD_NAME"
    - git config --global user.email "$CI_TECH_LEAD_EMAIL"
  script:
    - "make gen-python-doc gen-docs-doc package-config"
    - "cp -r ./lib/python/docs/build/html ./final/${DOC_VERSION}/python" # sphinx
    - "cp ./docs/.openapi/api.yaml ./final/${DOC_VERSION}/rest/api.yaml" # openapi
    - "cp ./docs/.openapi/swagger-ui.html ./final/${DOC_VERSION}/rest/index.html" # openapi
    - "cp ./docs/.openapi/custom.css ./final/${DOC_VERSION}/rest/custom.css" # openapi
    - "cp -r ./site/* ./final/${DOC_VERSION}" # mkdocs
    - "cp .docker/dist.tar.gz ./final/${APP_VERSION}/dist.tar.gz" # dist
    - "cp ./docs/index.html.tpl ./final/index.html" # redirect patch docs
    - "cp ./docs/*.pdf ./final/"
    - sed -i "s/DOC_VERSION/$DOC_VERSION/g" ./final/index.html
    - "bash ./.gitlab/gen-badge.sh"
    - eval $(ssh-agent -s)
    - "mkdir -p /root/.ssh"
    - echo "$CI_KEY_PRIVATE" > /root/.ssh/id_rsa && chmod 0600 /root/.ssh/id_rsa
    - echo "$CI_KEY_PUBLIC" > /root/.ssh/id_rsa.pub
    - echo "$CI_DOC_ID" > ~/.ssh/known_hosts
    - tar czf ./final.tar.gz ./final
    - "scp -oHostKeyAlgorithms=+ssh-rsa -oPubkeyAcceptedAlgorithms=+ssh-rsa final.tar.gz $CI_DOC_USER@$CI_DOC_IP:final.tar.gz"
    - "scp -oHostKeyAlgorithms=+ssh-rsa -oPubkeyAcceptedAlgorithms=+ssh-rsa versions.json $CI_DOC_USER@$CI_DOC_IP:/system/user/ifs/infrastructures/public_html/dbrepo/versions.json"
    - "scp -oHostKeyAlgorithms=+ssh-rsa -oPubkeyAcceptedAlgorithms=+ssh-rsa ./docs/index.html.tpl $CI_DOC_USER@$CI_DOC_IP:/system/user/ifs/infrastructures/public_html/dbrepo/index.html"
    - 'ssh -oHostKeyAlgorithms=+ssh-rsa -oPubkeyAcceptedAlgorithms=+ssh-rsa $CI_DOC_USER@$CI_DOC_IP "rm -rf /system/user/ifs/infrastructures/public_html/dbrepo/${DOC_VERSION}; tar xzf ./final.tar.gz; rm -f ./final.tar.gz; cp -r ./final/* /system/user/ifs/infrastructures/public_html/dbrepo; rm -rf ./final"'

release-libs:
  stage: release
  image: docker.io/python:${PYTHON_VERSION}-alpine${ALPINE_VERSION}
  only:
    - tags
  variables:
    PIPENV_PIPFILE: "./lib/python/Pipfile"
  before_script:
    - "pip install pipenv twine build"
    - "pipenv install --dev --system --deploy"
    - "echo ${CI_PIPYRC} | base64 -d > ./.pypirc"
  script:
    - "python -m build ./lib/python"
    - "python -m twine upload --config-file ./.pypirc --verbose --repository pypi ./lib/python/dist/dbrepo-*"

verify-install-script:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: verify
  only:
    - /^release-.*/
  variables:
    SKIP_CHECKS: 1
  before_script:
    - "apk add bash curl"
  script:
    - "curl -sSL https://gitlab.phaidra.org/fair-data-austria-db-repository/fda-services/-/raw/release-${DOC_VERSION}/.scripts/install.sh | bash | grep 'Success!'"

verify-dist:
  image: docker.io/alpine:${ALPINE_VERSION}
  stage: verify
  only:
    - /^release-.*/
  before_script:
    - "apk add curl"
  script:
    - "curl -v --output /dev/null --fail https://www.ifs.tuwien.ac.at/infrastructures/dbrepo/${APP_VERSION}/dist.tar.gz"

verify-images:
  image: docker.io/docker:${DOCKER_VERSION}-dind
  stage: verify
  only:
    - /^release-.*/
  before_script:
    - "apk add --no-cache wget bash"
    - "wget -O /usr/local/sbin/regctl https://github.com/regclient/regclient/releases/download/v${REGCLIENT_VERSION}/regctl-linux-amd64"
    - "chmod +x /usr/local/sbin/regctl"
  script:
    - bash ./.gitlab/check-supported-images.sh

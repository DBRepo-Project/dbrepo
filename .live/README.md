# How to Build

Do *NOT* attempt to build this script using `sudo`, it causes unintended side effects with the chroot-environment.

Instead:

```shell
su
# (type in your sudo password)

./build-live-iso.sh
```

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-db-shared
spec:
  storageClassName: {{ .Values.dataDb.persistence.sharedStorageClass }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
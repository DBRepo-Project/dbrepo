apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-db-shared
spec:
  storageClassName: {{ .Values.dataDbSidecar.persistence.storageClass }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi